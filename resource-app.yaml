apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-app
spec:
  selector:
    matchLabels:
      run: resource-app
  template:
    metadata:
      labels:
        run: resource-app
    spec:
      containers:
      - name: resource-app
        image: ireshmm/resource:2
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        resources:
          limits:
            cpu: 500m
          requests:
            cpu: 200m
---
apiVersion: v1
kind: Service
metadata:
  name: resource-app
  labels:
    run: resource-app
spec:
  ports:
  - port: 80
    targetPort: 5000
  selector:
    run: resource-app
